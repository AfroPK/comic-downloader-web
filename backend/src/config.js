// Central site configuration
// Set TARGET_SITE in your environment, or default to the configured value
const TARGET_SITE = process.env.TARGET_SITE || 'https://example-comic-site.com';

module.exports = { TARGET_SITE };
